Task 8 — macOS Tahoe Compatibility Audit
Generated: 2026-03-09

=== LINES COMMENTED OUT ===

Total: 7 lines commented out (prefixed with `# [TAHOE] Removed:`)

1. NotificationCenter launchctl unload (line ~92)
   Pattern: launchctl unload referencing com.apple.notificationcenterui
   Reason: NotificationCenter menu bar daemon removed in modern macOS

2. iTunes/rcd launchctl unload (line ~223)
   Pattern: launchctl unload referencing com.apple.rcd
   Reason: iTunes removed (replaced by Music.app); rcd daemon no longer exists

3. AppleFontSmoothing defaults write (line ~244)
   Pattern: defaults write NSGlobalDomain AppleFontSmoothing -int 1
   Reason: Font smoothing API removed in macOS Tahoe — writing this key has no effect and may emit warnings

4. Dashboard disable (line ~401)
   Pattern: defaults write com.apple.dashboard mcx-disabled -bool true
   Reason: Dashboard was removed in macOS 10.15 Catalina; key no longer recognized

5. Dashboard overlay (line ~404)
   Pattern: defaults write com.apple.dock dashboard-in-overlay -bool true
   Reason: Dashboard removed; this key no longer has any effect

6. tmutil disablelocal (line ~645)
   Pattern: hash tmutil &> /dev/null && sudo tmutil disablelocal
   Reason: `tmutil disablelocal` subcommand removed in macOS Sierra (10.12) and later

7. Dashboard devmode (line ~672)
   Pattern: defaults write com.apple.dashboard devmode -bool true
   Reason: Dashboard removed; key no longer recognized

=== LINES MODIFIED (not commented out) ===

1. System Preferences → System Settings (line 7)
   Original: osascript -e 'tell application "System Preferences" to quit'
   Changed:  osascript -e 'tell application "System Settings" to quit'
   Reason: App renamed to "System Settings" in macOS Ventura (13.x) and later

=== NEW SECTION ADDED ===

WindowManager section inserted before the "Dock, Dashboard, and hot corners" section:

  ###############################################################################
  # Window Manager (macOS Sequoia/Tahoe tiling)                                #
  ###############################################################################

  # Disable margins around tiled windows (prefer edge-to-edge tiling)
  defaults write com.apple.WindowManager EnableTiledWindowMargins -bool false

=== SUMMARY ===
- 7 lines commented out with `# [TAHOE] Removed:` prefix
- 1 line updated (System Preferences → System Settings)
- 1 new section added (WindowManager tiling)
- 0 lines deleted
- File structure and all other settings preserved
